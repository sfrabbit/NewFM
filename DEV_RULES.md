# NewFM 开发规范（强制执行）

## 一、改代码前必须做的事

1. **先读再改**：修改任何文件前，必须先用 Read 读取最新内容，绝不能凭记忆改
2. **确认影响范围**：改一个逻辑，必须搜索所有引用该逻辑的文件，全部同步修改
3. **绝不擅自改配置**：端口号、文件路径、URL等配置项，未经用户确认不得修改

## 二、改代码后必须做的事

1. **重启服务**：修改 engine/*.js 后必须重启服务器（node server/index.js）
2. **验证生效**：改完后必须通过 API 或页面验证修改确实生效
3. **检查端口**：确认用户实际访问的端口，不要自作主张改端口

## 三、属性/数据规范

1. **身高**：单位 cm，直接用高斯分布生成（均值+标准差），不 clamp，不转换
2. **其他属性**：1-20 整数范围
3. **前端显示**：身高显示 cm 后缀，输入框宽度足够三位数

## 四、文件修改同步清单

每次修改必须检查以下文件是否需要同步：
- `engine/presets.js` ← 模板数据
- `engine/player.js` ← 生成逻辑
- `engine/constants.js` ← 常量定义
- `engine/zones.js` ← 区域/阵型定义
- `engine/situations.js` ← 场景定义
- `engine/transitions.js` ← 场景转换
- `engine/match.js` ← 比赛引擎
- `public/fm-engine.js` ← 前端打包引擎（必须与 engine/ 同步）
- `public/index.html` ← 主页面
- `public/admin.html` ← 管理页面
- `server/index.js` ← 服务端 API

## 五、禁止事项

1. **禁止覆盖用户已确认的代码**：修改前必须备份
2. **禁止改端口/路径/URL**：除非用户明确要求
3. **禁止用转换函数代替直接生成**：身高直接生成cm，不要从1-20映射
4. **禁止改了后端不重启就认为生效**
5. **禁止猜测用户截图内容**：看不到图就问，不要瞎改
