# 工作手册 — 禁止事项

## ⛔ 禁止擅自调整参数

**任何情况下不得私自调整数值参数**（概率阈值、权重系数、衰减速率等），包括但不限于：
- `situations.js` 中的射门/传球/盘带概率阈值
- `shoot.js` 中的射正率、门将系数
- `pass.js` 中的成功率参数
- `match.js` 中的时间成本
- 所有模块中的硬编码数值

**正确做法**：
1. 测试后发现数据偏差 → **不要调参**
2. 分析根因：球员为什么做出这些决策？
3. 输出分析结果给用户，讨论是否需要改以及怎么改
4. 参数调整必须经用户确认

## ✅ 可以做

- 添加日志/诊断输出，帮助分析决策过程
- 创建分析脚本，展示不同球员类型的决策概率分布
- 修改决策**逻辑结构**（新增决策因子、改变计算方式）
- 修改代码bug
